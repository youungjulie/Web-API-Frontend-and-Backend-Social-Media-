//- reference the main-layout.pug file
extends layouts/main-layout.pug

block content
    //- add the post form to the home page
    +createPostForm(userLoggedIn)
    .postsContainer

    //- post modal includes reply and delete modals
    +createPostModals(userLoggedIn)

block scripts
    script(src="/js/home.js")